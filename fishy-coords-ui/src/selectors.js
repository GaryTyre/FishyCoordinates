export const stage = (store)=> ({
    stage: store.stage
});

export const apiURI = (store)=> ({
    apiURI: store.apiURI
});

export const vessels = (store)=> ({
    vessels: store.vessels
});

export const currentVessel = (store)=> ({
    currentVessel: store.currentVessel
});

export const trips = (store)=> ({
    trips: store.trips
});

export const currentTrip = (store)=> ({
    currentTrip: store.currentTrip
});

export const tripPings = (store)=> ({
    tripPings: store.tripPings
});

export const tripCatches = (store)=> ({
    tripCatches: store.tripCatches
});

export const currentPings = (store)=> ({
    currentPings: store.currentPings
});

export const currentCatches = (store)=> ({
    currentCatches: store.currentCatches
});

export const currentRegions = (store)=> ({
    currentRegions: store.currentRegions
});

export const mapFocus = (store)=> ({
    mapFocus: store.mapFocus
});

export const fishynessLevels = (store)=> ({
    fishynessLevels: store.fishynessLevels
});

export const pingsShown = (store)=> ({
    pingsShown: store.pingsShown
});
